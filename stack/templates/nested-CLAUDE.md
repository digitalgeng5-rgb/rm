# REPLACE_ME

<!-- Nested app CLAUDE — keep ≤60 lines. Local docs: ./docs/INDEX.md. Root: ../../CLAUDE.md -->

## What

One sentence: runtime role of this app.

## Owns

- Paths this workspace may change.
- Composition roots unique here.

## Never / Always

- **Never** … (local boundary — evidence `path:line`)
- **Always** … (local boundary — evidence `path:line`)

## Verify

```bash
# scoped commands for this workspace only
```

## Pointers

- Local docs: `docs/INDEX.md`, `docs/ARCHITECTURE.md`, `docs/GOTCHAS.md`
- Local surfaces/flows: `docs/llm/API_SURFACE.yaml`, `docs/llm/FLOWS.md`
- Root: `CLAUDE.md`, `docs/llm/MODULE_MAP.yaml` (`id: REPLACE_ME`)
