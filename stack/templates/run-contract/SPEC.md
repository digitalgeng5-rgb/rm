# Specification: {{slug}}

Project working directory: `{{project_cwd}}`

## Goal

REPLACE_ME one paragraph: the single product outcome this run delivers.

## Interfaces

REPLACE_ME stable APIs, routes, types, or module exports writers must honor.
Paste signatures or paths — not prose-only.

## Invariants

- REPLACE_ME what must keep working
- REPLACE_ME data/schema constraints (e.g. no prisma migration)

## Out of scope

- REPLACE_ME explicit non-goals (related systems that must not be touched)

## Definition of done

- REPLACE_ME observable acceptance (behavior + focused tests)
- L1 per task green; L2 full/affected suite once at pre-merge when multi-task
