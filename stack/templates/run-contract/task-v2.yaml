schema_version: 2
id: "001"
title: "REPLACE_ME concrete outcome"
risk: medium
lane: {{lane}}  # must match agents-doctor main_write (adoc)
project_cwd: {{project_cwd_yaml}}
read_first:
  - "AGENTS.md"
interfaces: []
invariants:
  - "Preserve unrelated user work"
out_of_scope: []
expected_outputs:
  - "REPLACE_ME product or code output created by this task"
owns_paths:
  - "REPLACE_ME/product-surface/**"
never_touch:
  - ".env*"
depends_on: []
objective: |
  REPLACE_ME requested behavior and smallest acceptable implementation.
acceptance:
  - "REPLACE_ME observable completion condition (behavior, not monorepo-green)"
verify: tests
# L1 only: focused commands for this task's owns_paths (unit/spec + package
# typecheck). Do NOT put full monorepo/package suites here — that is L2 once
# at pre-merge / CI for the whole run.
verification:
  # timeout_sec: omit — control plane defaults to 900s (do not invent in plans)
  - command: "REPLACE_ME focused test or typecheck for this task only"
    cwd: {{project_cwd_yaml}}
