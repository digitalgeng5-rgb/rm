# REPLACE_ME — local architecture

> Scoped to this app only. Do not paste the monorepo tree.  
> Fill every section from code — a 5-line stub fails validation.

## Purpose

What this runtime owns (2–4 sentences + evidence).

## Entrypoints

| Entry | Role | Evidence |
|-------|------|----------|
| `path` | … | `file:line` |

## Owns / does not own

| Owns | Does not own (delegate) |
|------|-------------------------|
| … | … |

## Boundaries / composition notes

- May import: …
- Must not import: …
- Ordering / auth / money / isolation contracts (`path:line`)

## Verify

```bash
# scoped workspace commands from package.json
```
