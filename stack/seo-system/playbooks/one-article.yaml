id: one-article
title: Produce one SEO article/page
when: "Single cluster/page delivery"
mode: any
chain:
  - module: intent-semantics
    scenario: single-query-latent
  - module: intent-semantics
    scenario: intent-chain
    optional: true
  - module: content-gist
    scenario: creation
  - module: content-quality
    scenario: post-gist-pipeline
  - module: content-gist
    scenario: meta-step8
notes: |
  Prefer X4 /страница if the page sits in a cluster. Latent Intent only for one isolated query.
  SERP evidence required before final page-type claim.
