#!/usr/bin/env bash
# Compatibility entrypoint; typed logic lives in night-review-engine.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "$SCRIPT_DIR/night-review-engine" review "$@"
