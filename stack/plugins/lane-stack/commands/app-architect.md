---
description: Architect a new app or service — plain-language chat, living plan artifacts
---

Load skill `app-architect`.

If `$ARGUMENTS` is `info` / `справка`: print that skill's info block, then stop.

Otherwise start the architect session in the current project cwd (or `$ARGUMENTS`
if it is a path). Do not `run-init`. Speak plain Russian. Write artifacts the
same turn.
