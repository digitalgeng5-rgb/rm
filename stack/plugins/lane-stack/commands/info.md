---
description: Lane-stack work-process cheat sheet (resume, plan, design, run, docs)
---

Load skill `info`. Render its info card **exactly** (markdown), then stop.
Do not start a run, onboard, or design extract.

If the user named a process (`project-design`, `resume`, …), load that skill
and print **its** info block instead.
